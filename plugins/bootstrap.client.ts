import 'bootstrap/dist/js/bootstrap.bundle.min.js';

// We need to add an export to this file to satisfy the Nuxt build process,
// even though we don't actually need to export anything from this file.
// See https://nuxt.com/docs/guide/concepts/plugins#client-side-only-plugins
export default defineNuxtPlugin(() => {});